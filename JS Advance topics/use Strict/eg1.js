'use strict';

(function () {
	name = 'world';
	console.log('hello ' + name);
})();
// output : will throw an error since name is not declared. : if there are errors use strict will abort the execution if used.
